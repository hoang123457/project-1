<MenuLayout>:
    orientation: 'vertical'
    padding: 10
    spacing: 10

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "JUMP AIMBOT :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "AIMLOCK HEAD :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "LIB2CPP MAX :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "AIMCHEAT VIP :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "SPEED HEADSHOT :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "WHITE444 MODE :"
        ToggleButton:
            text: "OFF"
            on_press: self.text = "ON" if self.text == "OFF" else "OFF"

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "SENSITIVITY AIM :"
        Slider:
            min: 0
            max: 100
            value: 49

    BoxLayout:
        size_hint_y: None
        height: 50
        Label:
            text: "AUX SENSITIVITY :"
        Slider:
            min: 0
            max: 100
            value: 73

    Button:
        text: "BYPASS"
        size_hint_y: None
        height: 50
        background_color: (0.6, 0, 0.6, 1)

    Button:
        text: "MỞ FREE FIRE"
        size_hint_y: None
        height: 50
        background_color: (0.6, 0, 0.6, 1)

    Button:
        text: "MỞ FF MAX"
        size_hint_y: None
        height: 50
        background_color: (0.6, 0, 0.6, 1)
